import { RemoveHyphenPipePipe } from './remove-hyphen-pipe-pipe';

describe('RemoveHyphenPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveHyphenPipePipe();
    expect(pipe).toBeTruthy();
  });
});
